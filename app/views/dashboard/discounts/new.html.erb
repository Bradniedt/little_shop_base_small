<%= form_for @form_path do |f| %>

  <% if @discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discount.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @discount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :discount_type %></p>
  <p><%= f.number_field :discount_type, required: true %></p>
  <p><%= f.label :amount %></p>
  <p><%= f.number_field :amount, required: true %></p>
  <p><%= f.label :quantity %></p>
  <p><%= f.number_field :quantity, required: true %></p>
  <p><%= f.submit "Create Discount" %></p>
<% end %>
