<%= form_for @form_path do |f| %>

  <% if @discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discount.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @discount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :discount_type %>
  <%= f.number_field :discount_type, required: true %>
  <%= f.label :amount %>
  <%= f.number_field :amount, required: true %>
  <%= f.label :quantity %>
  <%= f.number_field :quantity, required: true %>
  <%= f.submit "Create Discount" %>
<% end %>
